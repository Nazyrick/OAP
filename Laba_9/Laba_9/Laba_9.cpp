#include <iostream>
#include <time.h>
using namespace std;
int main()
{
	/*setlocale(LC_CTYPE, "Russian");
	const int n = 100;
	int i, sz, a[n], min;
	int rmn = 0, rmx = 99;
	cout << "введите размер массива "<<(" ")<<endl;
	cin >> sz;
	cout << "" << endl;
	srand((unsigned)time(NULL));
	
	for (i = 0; i < sz; i++) {
		a[i] = (int)(((double)rand() / (double)RAND_MAX) * (rmx - rmn) + rmn);
		cout << a[i] << (" ");
	}
	min = a[0];
	for (int i = 0;i < sz;i++) {
		if (a[i] < min)
			min = a[i];
			}
	cout << endl;
	for(i=0;i<sz;i++){
		if ((i+1) % 2 == 0) 
			a[i] = min;

		}
				
	cout << endl;
	for (i = 0; i < sz; i++) {
				cout << a[i] << (" ");
	}*/

	int i, u;
	int a[] = { 6778,6,4,6,444,0,7,778,3567,9876,1,0,4567,7654,3456,7654,0,2345,87654,2345,0};
	for (int i = 0;i < size(a);i++) {
		if (a[i] == 0) {
			cout << i << "\n";
			continue;
		}
		}

}
 
